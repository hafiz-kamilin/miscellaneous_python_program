#!/usr/bin/env python3
# -*- coding: utf-8 -*-  

# setting up module being used in the program
import sys

# check if python environment is Python 2
if sys.version_info[0] != 2:

    print ("\nThis codes only compatible with Python 2 environment!")
    exit()

# check if python environment is Python 3
if sys.version_info[0] != 3:

    print ("\nThis codes only compatible with Python 3 environment!")
    exit()